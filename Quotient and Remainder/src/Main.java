import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        System.out.println(Arrays.toString(Exercise.getQuotientAndRemainder(12, 4)));
        System.out.println(Arrays.toString(Exercise.getQuotientAndRemainder(10, 3)));
        System.out.println(Arrays.toString(Exercise.getQuotientAndRemainder(10, 0)));
    }
}
